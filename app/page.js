/**
 * Página principal (Home) — Ruta raíz del App Router.
 * El contenido visible del CV se renderiza desde el layout (Hero, Education, Skills, etc.).
 * Esta página queda vacía; el layout incluye todo el contenido en #content.
 */
export default function Home() {
    return null;
}